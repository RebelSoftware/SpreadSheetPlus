# Translations

Place compiled `.qm` translation files in this directory. FreeCAD looks them up
via the language path registered in `freecad/fcspreadsheet2/init_gui.py`.

To add a language, follow FreeCAD's "Translating an external workbench" guide:
https://wiki.freecad.org/Translating_an_external_workbench
